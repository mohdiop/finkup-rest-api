meta {
  name: Add fink
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/users/finks
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Fink API",
    "content": "In the api, users can authenticate and access to their finks",
    "category": "IDEA"
  }
}

settings {
  encodeUrl: true
}
