meta {
  name: Authenticate user
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth
  body: json
  auth: inherit
}

body:json {
  {
    "email": "example@example.com",
    "password": "Ex@mple123"
  }
}

settings {
  encodeUrl: true
}
